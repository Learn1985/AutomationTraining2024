package September8thSession;

public class StringBufferSession {

    public static void main(String[] args) {

        //StringBuilder and StringBuffer have same set of functions.
        //StringBuilder is not thread safe - Meaning all the
        //StringBuffer is thread safe and synchronized for slower performance
        //StringBuilder can let 10 users function parallelly at the same time.
        //StringBuffer can let only one user at a time to finish the action even if 10 users access at the same time

            //MethodDeclarationSession.method1();//Cannot be called given the method is private


    }
}
